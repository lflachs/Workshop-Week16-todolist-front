{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "jest --no-cache  --detectOpenHandles --runInBand --forceExit",
		"test:watch": "jest --no-cache  --detectOpenHandles --runInBand --forceExit --watch ",
		"start": "nodemon src/app",
		"migrate:test": "dotenv -- bash -c 'DATABASE_URL=$DATABASE_URL_TEST ; npx prisma migrate deploy --preview-feature'"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@prisma/client": "^2.13.1",
		"cors": "^2.8.5",
		"express": "^4.17.1"
	},
	"devDependencies": {
		"@prisma/cli": "^2.13.1",
		"faker": "^5.1.0",
		"jest": "^26.6.3",
		"supertest": "^6.0.1"
	}
}
